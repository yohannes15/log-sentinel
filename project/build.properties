# 1.12+ required for `console` with Scala 3.8 (REPL split out of scala3-compiler; see sbt#8349)
# Match the version baked into the devcontainer image so sbt can start without a network fetch.
sbt.version=1.12.8
